config:
  target: "http://localhost:6969/chat"
  phases:
    - duration: 300
      arrivalRate: 1000
      maxVusers: 1000
    - duration: 60
      arrivalRate: 10000
    - duration: 240
      arrivalRate: 1000
      maxVusers: 1000
scenarios:
  - engine: "ws"
    flow:
      - send: '{"type":"message","body":"hello"}'
      - think: 0.1
