config:
  target: "http://localhost:6969/chat"
  phases:
    - duration: 120
      arrivalRate: 500

scenarios:
  - engine: "ws"
    flow:
      - send: '{"type":"handshake","id":"{{ $uuid }}" }'
      - think: 0.1
      - send: '{"type":"bye"}'
      - think: 0.05
